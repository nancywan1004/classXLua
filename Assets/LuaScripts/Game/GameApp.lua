local GameApp = {}

function EnterLoginScene()
    -- release game map to scene
    print("Enter Game Scene!")
    -- release character, monster, props, objects to scene

    -- release UI

end


GameApp.EnterGame = function ()
    EnterLoginScene()
end

return GameApp